# Limitations and deviations

This route audits availability and schema only. It does not replace the paper's SBERT-384/PCA-20/CNF/Sinkhorn/MFPCA pipeline, and it cannot verify alarm/event alignment. Even if it locates an exact stream, a child experiment must execute and independently check the complete pipeline before Claim 5 can change from `BLOCKED`.
