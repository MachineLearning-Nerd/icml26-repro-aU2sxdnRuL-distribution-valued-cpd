# Claim 4 current status

Route 1 only removes or confirms the data-access blocker. The performance claim remains BLOCKED until a later route runs the paper protocol or establishes a valid assumption-satisfying counterexample. Current code is `src/claim4_flowrepository_probe.py` with fail-closed checker `src/verify_claim4_flowrepository_probe.py`.
