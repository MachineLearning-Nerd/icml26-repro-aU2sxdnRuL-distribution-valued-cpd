# Limitations and deviations

- This is a bounded availability probe, not an IDD or Hotelling experiment.
- It uses the primary archive and embedded official AML variable, not the obsolete FlowCAP-II landing page.
- TLS certificate verification is disabled only for this legacy primary server; exact URLs and content hashes are recorded.
